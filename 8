sayi = int(input("Bir sayı girin: "))
durum = "Sayı Asal Sayı"

if sayi == 1:
    print("ASAL DEĞİL!")
else:
    for i in range(2, sayi // 2 + 1):
        if sayi % i == 0:
            durum = "Sayı Asal Sayı Değil!"
            break

    print(durum)
